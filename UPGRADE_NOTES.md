# 自定义 SKILL v2 修改说明

本次保留四个现有命令名，避免破坏仓库链接、用户习惯和已有工作流。修改集中在职责边界、证据记录、状态衔接和执行安全上。

## 共同变化

- 新增 `skills/custom/_shared/RESEARCH_ARTIFACT_CONTRACT.md`，统一项目状态、事件日志、交接包、实验运行和审计报告的目录与字段。
- 所有 SKILL 明确区分事实、推断、假设、决定和未知信息。
- 代码版本、配置、数据划分、运行命令和原始结果成为默认溯源字段。
- 模型意见被定义为辅助信号，不再等同于事实验证。
- 未经授权不提交代码、不启动高成本实验、不扩大任务范围。

## context-handoff-checklist

原版问题：

- 所有任务使用同一份固定清单；
- 强调背景完整，但缺少提交、运行、证据位置和修改边界；
- 只要求发送方自检，没有接收方确认。

v2：

- 先识别实现、调试、代码审查、实验分析等任务类型，再按需补材料；
- 增加仓库状态、文件位置、运行编号、事实/假设、允许修改范围和验证标准；
- 加入接收确认，要求接收方先复述任务、证据和计划。

## auto-discovery-logger

原版问题：

- 固定“偏差超过 10%”不适用于所有指标；
- 观察、解释和猜测混在同一条“发现”中；
- 自动追加容易重复，且缺少代码、配置和数据溯源。

v2：

- 改为按实验计划、统计波动和研究影响判断是否记录；
- 每条事件分离 observation、interpretation、hypothesis、alternative explanations 和 decision；
- 使用追加式 JSONL，支持去重、状态迁移和修正引用；
- 只有复现或有足够证据的事件才写入项目当前状态。

## ai4ai-model-optimizer

原版问题：

- 默认声称自动完成诊断、改代码、训练和重评；
- 轮数、GPU 时长、模型版本等常量写死；
- 一处允许合并多个改动，另一处要求单一改动；
- 让模型估计具体指标提升，依据不足；
- 没有明确限制最终测试集调参。

初始 v2（历史口径）：

- 默认 `plan-only`，明确授权后才进入 `execute`；
- 预算、数据划分、允许修改范围和停止条件由用户或实验计划提供；
- 先检查基线、评估和数据有效性，再诊断性能；
- 每项实验围绕可证伪假设设计，原则上只改变一个主要因素；
- 禁止用最终测试集驱动迭代；
- 保留负面和无变化结果，不承诺指标提升。

后续职责校正：初始 v2 将安全收缩做得过头，已经偏离“Agent 自动调优超参数”的原始目标。当前版本改为：

- 使用 `plan / tune / resume / report` 四种模式；未指定模式时为 `plan`；
- 旧 `plan-only` 可以映射到 `plan`；旧 `execute` 不会静默运行，必须先转换为完整的 `tune` 规格；
- `tune` 在用户确认的搜索空间、验证指标、预算和停止条件内自动生成、运行和比较 trial；
- 支持网格、随机、TPE/Bayesian、Agent 自适应和混合搜索；
- trial 默认按参数组变化，允许联合搜索已声明的耦合参数；
- 使用 `.research/tuning/<tuning_id>/` 保存冻结规格、可恢复状态、追加式 trial 账本和最佳配置；
- top-k 配置经多 seed 确认后才产生 `best_config.yaml`；最终测试集只在单独授权后评估一次；
- 新 trial 必须先按单 trial 上限预留名额和资源，保证全局 deadline、算力与费用硬预算不会被运行中任务突破；
- 最终测试授权在搜索结束后追加到独立账本，不改写冻结规格；sampler、active attempt 和多 seed config 聚合状态均可恢复、可审计；
- 默认仍不修改架构、数据、划分或评价代码，也不自动扩大搜索空间和预算。

## cross-model-verifier

原版问题：

- “验证”容易让人误以为多个 LLM 意见可以证明结果；
- 虽然包含独立重算，但模型审查的存在感过高；
- 固定审阅模型，仓库描述与实际支持的模型可能不一致；
- 使用统一的 VERIFIED 标签，无法区分重算、代码检查和观点支持。

v2：

- 先做运行对应、独立重算、数据泄漏、基线公平性和统计检查；
- 模型审查放在确定性检查之后，并保持彼此独立；
- 不按模型票数裁决分歧；
- 使用 RECOMPUTED、CHECKED、SUPPORTED、UNRESOLVED 等证据等级；
- 只有实际调用并记录版本的模型才能写入报告。

## 建议的 README 描述

| Skill | 解决的问题 |
|---|---|
| `context-handoff-checklist` | 为跨 Agent、跨模型和跨会话任务生成带代码版本、证据位置与操作边界的交接包 |
| `auto-discovery-logger` | 将实验中的观察、假设、决定和负面结果记录为可追溯事件 |
| `ai4ai-model-optimizer` | 在固定数据划分和显式预算内自动生成、运行和比较超参数 trial，支持规划、调优、恢复与报告 |
| `cross-model-verifier` | 先独立重算并检查数据与评估，再用不同模型补充审查和分歧分析 |

## 集成审计修正

- 三个记录/交接/审计 skill 只预批准只读工具；optimizer 为连续调优预批准 Bash、Write 和 Edit，但禁止模型自动触发，并以显式 `tune`、冻结规格和硬预算作为执行闸门。
- `auto-discovery-logger` 的候选事件使用已定义的 `status: unknown`，不再引入未声明的 `candidate` 状态。
- `cross-model-verifier` 将审计等级记录在 `audit_grade`，写回共享状态时使用显式映射。
- 向外部模型发送证据前必须得到授权，并最小化、脱敏材料。
- 安装脚本使用目录内容合并，重复运行会更新现有技能而不会产生嵌套目录。
